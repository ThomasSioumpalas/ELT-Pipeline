models:
  - name: fct_orders
    columns:
      - name: order_key
        tests:
          - unique
          - not_null
      - name: status_code
        tests:
          - accepted_values:
              arguments:
                values: ["P", "O", "F"]
